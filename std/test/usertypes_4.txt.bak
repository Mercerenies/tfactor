
# Let's see if type names actually canonicalize like they should.

mod Module1
  record Foo
    constructor foo
  end
end

mod M
  mod Module2 = Module1 end
  mod Bar = Module1.Foo end
end

record DistinctType
  constructor dt
end

fun convert1 ( Module1.Foo -- M.Module2.Foo ) end
fun convert2 ( M.Module2.Foo -- M.Bar ) end
fun convert3 ( M.Bar -- Module1.Foo ) end

# fun failedconvert ( Bar -- DistinctType ) end

fun test ( -- Bool )
  true
end
